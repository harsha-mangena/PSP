server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router uses client-side routing (BrowserRouter), so any path that
    # isn't a real static file must fall back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
