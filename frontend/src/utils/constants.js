/**
 * The exercise has no authentication layer, so the cart is scoped to a fixed
 * demo user. In a real system this would come from the auth context.
 */
export const DEMO_USER_ID = 'demo-user'

export const PAGE_SIZE_OPTIONS = [5, 10, 20]
